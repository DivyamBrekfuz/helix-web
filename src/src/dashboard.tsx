// Responsive time-series chart. Must not overflow the viewport on <400px screens (#12).
export function Dashboard({ workspaceId }: { workspaceId: string }) {
    // p95 target < 800ms via covering index on (workspace_id, metric, bucket).
  return null;
}
